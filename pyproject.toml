[project]
name = "journey-weather"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.1",
    "ruff>=0.11.10",
]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
]
